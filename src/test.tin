import collections/List

Cat = type:
	name: String = "Unknown Cat"

Third = type:
	label: String

CatMeow = type:
	meow = (this: Cat) ->
		if this :: Third, 
			print("THIRD {this.label}")
		else
			print("Hello")

catProto = Cat("Kitty")
cat = catProto & CatMeow() & Third("New")
cat.meow()

a = arrayOf[Number](1, 2, 3, 4)
l = listOf[Number](1, 2, 3, 4, 5)
print(stringOf(l))

