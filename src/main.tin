# import collections/List

Box = data:
	value: var Number

box: Anything = Box(var 12)
vbox: Anything = Box(var 21)

takeInvar = (v: Number) ~> print(v)

takeInvar(var 2)

f = () ~>
	if box :: Box,
		print("Yes1")

f2 = () ~>
	if vbox :: Box,
		print("Yes2 {vbox.value}")


f()
f2()