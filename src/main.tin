# import collections/Map
import collections/List

Animal = data:
	breed: String

Furry = data:
	something: Number

Cat = Animal & Furry & data:
	name: String

cat = Animal("Something") & Furry(2) & Cat("Mercy")

animal = Animal("Fwoof")
arr = Array@of(1, 2, 3)

print(arr)
